import { ValidateFormThree } from './validate-form-three';

describe('ValidateFormThree', () => {
  it('should create an instance', () => {
    expect(new ValidateFormThree()).toBeTruthy();
  });
});
